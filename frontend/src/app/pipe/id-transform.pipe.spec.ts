import { IdTransformPipe } from './id-transform.pipe';

describe('IdTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new IdTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
